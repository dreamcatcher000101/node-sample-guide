import setupDatabase from "./database.setup";
import setupBackend, { App } from "./backend.setup";
import setupTestDB from "./testDB.setup";

export { setupDatabase, setupBackend, setupTestDB, App };
